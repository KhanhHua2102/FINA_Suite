{
  "name": "fina-suite",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd fina_portal/backend && ./venv/bin/python -m app.main\" \"cd fina_portal/frontend && npx vite\"",
    "dev:backend": "cd fina_portal/backend && ./venv/bin/python -m app.main",
    "dev:frontend": "cd fina_portal/frontend && npx vite"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
